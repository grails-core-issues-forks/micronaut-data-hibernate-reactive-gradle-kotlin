applicationType: default
defaultPackage: example.micronaut
testFramework: junit
sourceLanguage: kotlin
buildTool: gradle
features: [app-name, awaitility, data, data-hibernate-reactive, graalvm, gradle, http-client-test, junit, kotlin, kotlin-application, ksp, logback, micronaut-aot, micronaut-build, micronaut-http-validation, mysql, netty-server, reactor, readme, serialization-jackson, shade, spotless, static-resources, test-resources, validation, yaml, yaml-build]
